# Hello world annotation processor
Modifies AST, adding main method with "Hello, World!" message to classes, annotated with `@HelloWorld`.

Compile `TestClass.java` with `javac -cp ../hello-world.jar`.